import { Button } from 'ui-kit';

const MainPage = () => {
    console.log(import.meta.env);
    return (
        <div>
            <Button />
            <div></div>
        </div>
    );
};

export default MainPage;
